<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
        <loc>{{ url_for('main.index', _external=True) }}</loc>
        <changefreq>weekly</changefreq>
        <priority>1.0</priority>
    </url>
    <url>
        <loc>{{ url_for('main.about', _external=True) }}</loc>
        <changefreq>monthly</changefreq>
        <priority>0.8</priority>
    </url>
    <url>
        <loc>{{ url_for('main.radar', _external=True) }}</loc>
        <changefreq>weekly</changefreq>
        <priority>0.7</priority>
    </url>
    <url>
        <loc>{{ url_for('main.notas', _external=True) }}</loc>
        <changefreq>weekly</changefreq>
        <priority>0.6</priority>
    </url>
    {% for tag in tags %}
    <url>
        <loc>{{ url_for('main.tag', tag_name=tag.name, _external=True) }}</loc>
        <changefreq>monthly</changefreq>
        <priority>0.5</priority>
    </url>
    {% endfor %}
    {% for post in posts %}
    <url>
        <loc>{{ url_for('main.post', post_id=post.id, _external=True) }}</loc>
        <lastmod>{{ post.created_at.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>yearly</changefreq>
        <priority>0.9</priority>
    </url>
    {% endfor %}

</urlset>
